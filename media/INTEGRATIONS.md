# Integrations

Per [PRINCIPLES.md](PRINCIPLES.md) #8: a row exists only for a provider config and model actually run in this working tree. The "what was verified" column cites the test, fixture, or command that proved it, with the date it ran. A model or endpoint not in this table has not been run here and is not a recommendation, whatever its model card or marketing claims.

| integration | provider config | model | what was verified | date | off-machine |
|---|---|---|---|---|---|
| static | `{"provider": "static"}` | minishlab/potion-retrieval-32M | parity fixtures (`test/fixtures/parity/`) reproduce the reference model2vec output within float tolerance; full test suite passes; nfcorpus eval reproduces the documented retrieval numbers digit-for-digit; English-only scope measured: fused retrieval is net-negative on MIRACL zh/ja/ru/de (benchmark/reports/2026-08-27-embedding-model-selection.md) | 2026-08-26/27 | no |
| openai | `{"provider": "openai", "url": "<endpoint>/v1"}` | mock server | request/response shape and 429 retry/backoff, against a local mock HTTP server, in `test/integration/embed.test.ts` and `test/unit/embed/openai.test.ts`/`test/unit/embed/http.test.ts` | 2026-08-26 | no (mock ran on localhost) |
| cohere (live) | `{"provider": "cohere", "key": "COHERE_API_KEY"}` | embed-v4.0 | live API: auth, dims probe, doc/query `input_type` round-trip (`test/integration/live.test.ts` "cohere live", gated on `SENSE_TEST_COHERE_KEY`), plus a full CLI vault smoke where a French note ranked first for an English query via vector; mock-server shape tests remain alongside | 2026-08-27 | yes (tree content sent to Cohere) |
| openai (live Ollama) | `{"provider": "openai", "url": "http://localhost:11434/v1"}` | granite-embedding:30m | full nfcorpus bake-off through the shipped provider: fused +0.0419 nDCG / +0.0526 hit@10 over BM25 at native dims, the only model measured to clear both legacy fusion thresholds (benchmark/reports/2026-08-27-embedding-model-selection.md) | 2026-08-27 | no (local daemon) |
| openai (live Ollama) | `{"provider": "openai", "url": "http://localhost:11434/v1"}` | qwen3-embedding:0.6b | nfcorpus plus MIRACL zh/ja/ru/de bake-offs; cosine-only nDCG 0.73-0.79 on zh vs BM25's 0.49; measured without its optional query instruction, so a floor (benchmark/reports/2026-08-27-embedding-model-selection.md). Re-runnable: `test/integration/live.test.ts` "ollama live" embeds an en/zh/ja/ru/de tree through the shipped provider and asserts each language's query reaches the note that paraphrases it, plus the EMBED_MODEL_MISMATCH refusal when the config declares English only | 2026-08-27 | no (local daemon) |
| openai (live LM Studio) | `{"provider": "openai", "url": "http://localhost:1234/v1"}` | text-embedding-nomic-embed-text-v1.5 (LM Studio's bundled Q4_K_M gguf, present on any install, so this row needs no download) | `test/integration/live.test.ts` "lmstudio live": dims probe and a batch through the shipped provider, an English tree whose paraphrasing query reaches the right note via vector, and the EMBED_MODEL_MISMATCH refusal on a Chinese tree declared English-only. Run without nomic's `search_document:`/`search_query:` prefixes (the provider is symmetric) and at Q4_K_M, so its English retrieval here is a floor twice over | 2026-08-27 | no (local server) |
| openai (live LM Studio) | `{"provider": "openai", "url": "http://localhost:1234/v1"}` | bge-m3 (gpustack/bge-m3-GGUF, Q8_0, 635 MB) | `test/integration/live.test.ts` "lmstudio live" with `SENSE_TEST_LMSTUDIO_LANGUAGES=en,zh,ja,ru,de`: each language's tree embeds and its paraphrasing query reaches the note that paraphrases it via vector, plus the EMBED_MODEL_MISMATCH refusal when the config declares English only. bge-m3 needs no query prefix, so unlike the nomic and qwen3 rows this one is not a floor | 2026-08-27 | no (local server) |

The `off-machine` column says whether tree content left this machine. Only the Cohere row is `yes`; the static loader, the mock server, and the local Ollama and LM Studio servers all ran without tree content leaving the machine. Every row added for a hosted service carries that fact in the column, never silent.

Not run here, so not named as recommendations anywhere in this repo's docs: Cloudflare Workers AI, Jina, Voyage. Each is expected to work through the `openai` or `cohere` provider config shape, but expected is not verified. Rows for these, and for per-language model comparisons, follow once each is actually run.

Adding a row: append one line naming the provider config, the model, exactly what was checked and against what (live endpoint vs. mock), and the date. A row whose verification is a live endpoint gets a gate variable in `test/integration/live.test.ts` so the claim is reproducible rather than remembered; RELEASING.md step 1 lists the variables. The gate existing is not a claim; the row is.
